go to https://www.python.org/downloads/
after downloading

run installer. leave defaults.
after installing open cmd and type 
"pip install windows-curses"
(to get the colors and clearing the screen etc)
then click letter.py and it should run just fine.